---
description: Handoff MOVI KIDS — continuidade com mensagem mínima do usuário; ler docs ativos antes de qualquer trabalho
alwaysApply: true
---

# Continuidade MOVI KIDS

## Mensagem mínima do usuário (suficiente)

Se o usuário disser apenas algo como **"vamos dar continuidade ao projeto Movi Kids"**, **"continuar o Movi Kids"**, **"pasta no C da minha máquina"** ou equivalente — **não pedir** caminho, handoff, versão nem anexos.

**Ação imediata (sem perguntas):**

1. Confirmar que este workspace é o repo MOVI KIDS (`README.md` + `AGENTS.md` na raiz, ou pasta `movikids-github`).
2. Se o workspace for outro: abrir/usar `C:\Users\riboc\Documents\Codex\2026-05-30\files-mentioned-by-the-user-movikids\movikids-github`.
3. Ler os 5 documentos abaixo **antes** de propor ou executar qualquer tarefa.
4. Resumir em 3 linhas: produção (FE/GAS), **modo operação** (FASE 0–5 fechadas), próximo passo em `PLANEJAMENTO_ATUAL_2026-06.md`.
5. Perguntar só se o usuário quiser **desviar** do próximo passo — não por falta de contexto.

**GitHub:** `ribocg-a11y/movikids` · branch `main`

---

Ao trabalhar neste repositório, **ler primeiro** (nesta ordem):

1. `docs/ativos/HANDOFF_NOVO_CHAT.md` — produção, próximo passo, armadilhas
2. `docs/ativos/PLANEJAMENTO_ATUAL_2026-06.md` — backlog P0–P4 pós-FASE 5
3. `docs/ativos/PLANO_PRIORIDADES_2026-06.md` — fases concluídas
4. `docs/ativos/ESTADO_ATUAL.md` — versões e links canônicos
5. `docs/ativos/REGRAS_DE_PUBLICACAO_SEGURA.md` — antes de commit/push/deploy
6. `docs/ativos/ACESSOS_E_AUTORIZACOES.md` — papéis, PIN admin, agente vs humano

Índice: `docs/INDICE.md` · Entrada raiz: `AGENTS.md` · `README.md`

## Agente vs usuário (memória rápida)

**Agente SOZINHO:** ler docs, editar FE/docs, `pre-push-check` → commit → push FE, ping GAS, testes `scripts/testes/`, validar versões FE, **planilha Google** (OAuth), PRs, APIs admin, limpar testes planilha.

**Agente SÓ COM SEU PEDIDO (lista fechada §7.3 — única exceção):**

1. `clasp push` / `prepare-gas-push.ps1`
2. Mudar `api()`, auth, PIN, perfis
3. Reativar F4 (WhatsApp/SMS) ou F9 (supervisor)
4. Mudar código Apps Script (`.gs` canônico) ou reimplantar Nova versão Web GAS

**SÓ O USUÁRIO (limite físico):** tablet balcão **na loja**, Script Properties SMS, re-auth OAuth (`npm run auth`), `clasp login` se expirar.

**DOIS APARELHOS:** usuário opera sempre do **computador**; tablet fixo na **operação** com operadores. Prints no chat = PC; homologação Turno/timer/idle = tablet físico do balcão. Ver HANDOFF § Modelo operacional.

**Planilha:** ID `1ULMUx8AqZkZ75Ed0iRK_lQWc3I7YV9Itfoe-1JY5618` — scripts: `test-movikids.js`, `auditar-planilha-movikids.js`, `limpar-testes-movikids.js`, `criar-aba-investimento-movikids.js`. Ver `ACESSOS_E_AUTORIZACOES.md` §7.8.

**NUNCA:** `clasp deploy`, POST browser (I15), commit de segredos.

**I20 cronômetro (RESOLVIDO):** GAS v1.5.66+ + FE v1.7.78+ — doc `docs/ativos/INCIDENTE_I20_CRONOMETRO_RESOLUCAO_2026-06-07.md`. Não regredir sem `TESTE_I20_COMPLETO_PROD.ps1` + tablet.

**FASE 5:** ✅ fechada 09/06 — Milena + I21 v1.7.96 · ver `PLANEJAMENTO_ATUAL_2026-06.md`

**Produção (14/06/2026 — repo):** GAS **v1.5.91** prod. · FE **v1.8.16** · FOLHA OK (I25 fechado)

**"Atualize tudo":** ver `.cursor/rules/atualize-tudo-movikids.mdc` + `docs/ativos/PROTOCOLO_ATUALIZAR_TUDO.md`

**Teste / diagnóstico:** `PROTOCOLO_DIAGNOSTICO_E_TESTES.md` → `TESTE_PROTOCOLO_DIAGNOSTICO.ps1`.

**Regra 16:** toda resposta termina com `Mudança no AppScript: sim|não` + link `.gs` — `.cursor/rules/gas-deploy-caminho-pc.mdc`

**I20 guards:** timer → `-Foco cronometro` + tablet ▶→10:00.

## Modo de operação — máximo potencial (obrigatório)

1. Fluxo F0–F14 antes de codar · 2. PC ≠ tablet · 3. FE+GAS+planilha+PWA · 4. MAPA_ERROS I15/I18/I20/I21/I22/I23.

**I22 (09/06):** nunca push FE crítico com locações Ativa/Pendente — `check-operacao-livre.ps1` · Regra 14.

**I23 (09/06):** mutex KPI hub/dash separado; GAS `resumoDia` leve — Regra 15 · doc `INCIDENTE_I23_DASHBOARD_LENTO_TRAVADO_2026-06-09.md`.

Antes de push: `pre-push-check.ps1` + resumo publicação. Ao encerrar: HANDOFF + PLANEJAMENTO se mudou fase.

## Não usar como fonte de verdade

- `docs/arquivo/planos/HANDOFF_NOVO_CHAT_2026-06-05.md` (defasado)
- `docs/arquivo/obsoleto/`
- `arquivo-historico/*.gs` (GAS legado)

## Ao mudar versão ou fase

Atualizar `HANDOFF_NOVO_CHAT.md` + checklist em `PLANO_PRIORIDADES` + `ESTADO_ATUAL.md` se aplicável.

**"Atualize tudo":** checklist completo em `PROTOCOLO_ATUALIZAR_TUDO.md` (handoff, mapa erros, protocolos, arquitetura, planilhas, histórico, pasta C).
