# FinanceiroGeral (FASE 11 — referência)

Dashboard Movi + ZapClin — **não** faz parte do deploy MOVI KIDS balcão.

| Artefato | Uso |
|----------|-----|
| `FinanceiroGeral.gs` | GAS `controleFinanceiro` — referência FASE 11 |
| `index.html` · `app.js` | GitHub Pages em `/financeiro/` |

Deploy MOVI KIDS: ver `docs/ativos/DEPLOY_ATUAL.md`.
